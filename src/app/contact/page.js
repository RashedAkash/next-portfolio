import Contact from '@/components/Contact';

export const metadata = {
  title: "Contact",
  description: "This is contact page",
};

const page = () => {
  return (
    <div>
     
      <div>
        <Contact />
    </div>
    </div>
  );
};

export default page;